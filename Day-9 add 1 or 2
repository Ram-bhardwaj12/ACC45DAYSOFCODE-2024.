#include <stdio.h>

int main() {
    int T, N;
    scanf("%d", &T);
    for (int i = 0; i < T; i++) {
        scanf("%d", &N);
        if (N % 2 == 1) {
            printf("ALICE\n");
        } else {
            printf("BOB\n");
        }
    }
    
    return 0;
}
